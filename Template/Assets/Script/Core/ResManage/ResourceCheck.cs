using UnityEngine;
using System.Collections;

namespace GameBase
{
    public class ResourceCheck : MonoBehaviour
    {
        #region 固有函数
        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {
            //GameCore.Instance.ResourceManager.CheckProcess();
        }
        #endregion
    }
}
